{%- from "opencontrail/map.jinja" import control with context %}
IFMAP_USER={{ control.bind.address }}.dns
IFMAP_PASWD={{ control.bind.address }}.dns
COLLECTOR={{ control.bind.address }}
COLLECTOR_PORT=8086
DISCOVERY={{ control.master.host }}
HOSTIP={{ control.bind.address }}
CERT_OPTS=
LOGFILE=--log-file=/var/log/contrail/dns.log
LOG_LOCAL=
