{%- from "opencontrail/map.jinja" import control with context %}
IFMAP_USER={{ control.bind.address }}
IFMAP_PASWD={{ control.bind.address }}
COLLECTOR={{ control.bind.address }}
COLLECTOR_PORT=8086
DISCOVERY={{ control.master.host }}
HOSTNAME={{ pillar.system.name }}
HOSTIP={{ control.bind.address }}
BGP_PORT=179
CERT_OPTS=
LOGFILE=--log-file=/var/log/contrail/control.log
LOG_LOCAL=
